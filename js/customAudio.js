const audio=document.getElementById('customAudio');const playPauseBtn=document.getElementById('playPauseBtn');const seekBar=document.getElementById('seekBar');const volumeBtn=document.getElementById('volumeBtn');const volumeSlider=document.getElementById('volumeSlider');const volumeControl=document.getElementById('volumeControl');playPauseBtn.addEventListener('click',()=>{if(audio.paused){audio.play();playPauseBtn.textContent='■'}else{audio.pause();playPauseBtn.textContent='▶'}});audio.addEventListener('ended',()=>{playPauseBtn.textContent='▶';seekBar.value=0});audio.addEventListener('timeupdate',()=>{seekBar.max=audio.duration;seekBar.value=audio.currentTime});seekBar.addEventListener('input',()=>{audio.currentTime=seekBar.value});audio.addEventListener('ratechange',()=>{if(audio.playbackRate!==1){audio.playbackRate=1}});document.addEventListener('contextmenu',e=>{if(e.target===audio)e.preventDefault();});volumeBtn.addEventListener('click',()=>{volumeSlider.style.display=volumeSlider.style.display==='block'?'none':'block'});volumeControl.addEventListener('input',()=>{audio.volume=volumeControl.value});document.addEventListener('click',(e)=>{if(!volumeBtn.contains(e.target)&&!volumeSlider.contains(e.target)){volumeSlider.style.display='none'}})